const GraphQLResolvers = require('./resolvers');
const GraphQLTypeDefs = require('./typeDefs');
// const { SessionDirectives, SessionTypeDefs } = require('./directives');

module.exports = {
  GraphQLResolvers,
  GraphQLTypeDefs,
  // SessionDirectives,
  // SessionTypeDefs,
};